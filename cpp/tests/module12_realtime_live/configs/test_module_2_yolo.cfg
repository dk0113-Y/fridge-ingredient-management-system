# Test-only config for the module12 live harness.
# The same file is read by both the runtime and diff-analysis loaders.

model_path = models/best.onnx
class_names = fruit_vegetable, meat_egg_fresh, drink, packaged_food
input_width = 640
input_height = 640
score_threshold = 0.25
nms_threshold = 0.45
output_columns = 6

confidence_threshold = 0.50
iou_match_threshold = 0.30
center_distance_threshold = 0.45
partial_area_change_threshold = 0.20
partial_visual_delta_threshold = 18.0
uncertain_unmatched_ratio_threshold = 0.50
