# TC03_live_take_out

Scene:
- Start with one obvious large object already inside the ROI.
- Remove it after the live preview is stable.

Goal:
- Stage 1 should capture valid `before` and `after`.
- Final result should be `take_out`.

Suggested command:
```bash
cpp/tests/module12_realtime_live/scripts/run_live_module12_test.sh --case-id TC03_live_take_out --module2-mode mock
```
