# TC02_live_put_in

Scene:
- Start with the ROI clear or with stable background content.
- Put one obvious large object into the ROI after the live preview is stable.

Goal:
- Stage 1 should capture valid `before` and `after`.
- Final result should be `put_in`.

Suggested command:
```bash
cpp/tests/module12_realtime_live/scripts/run_live_module12_test.sh --case-id TC02_live_put_in --module2-mode mock
```
