# Module 1: Event Capture

This module contains the first-stage event capture chain:

- local video or frame-directory loading
- fridge ROI motion summary
- stable `before` and `after` frame selection
- runtime config loading for ROI and thresholds

Current files live under:

- `include/`
- `src/`
