# Python Model Tools

This directory stores offline Python utilities that support the C++ pipeline.

Current tool:

- `export_yolo_onnx.py`: export a YOLO `.pt` model to `.onnx` for later C++ integration

Current output:

- `models/best.onnx`: exported from `models/best.pt`
